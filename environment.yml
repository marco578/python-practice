name: base
channels:
  - defaults
dependencies:
  - python=3.10
  - conda==23.1.0
  - setuptools
  - pip
  - wheel
  - conda-content-trust
  - menuinst
  - console_shortcut_miniconda
  - powershell_shortcut_miniconda
  - tqdm
prefix: C:\Users\marco.scotto\AppData\Local\miniconda3
